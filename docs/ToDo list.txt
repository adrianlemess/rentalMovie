
M�todos e l�gicas
registerUser(user)
	- Verificar se o usu�rio ja existe
	- Verificar se o email j� existe
	- Verificar se a senha atende a requisitos minimos
	- Criptografar senha
login(email, password)
	- Verificar usu�rio
	- Logar usu�rio
logoff(user)
	- Tirar sess�o do usu�rio 
availableMoviesList()
	- Listar filmes dispon�veis
	- Negar acesso caso o usu�rio n�o esteja logado
rentMovie(idMovie)
	- Alugar um filme
	- Retornar erro caso o filme n�o esteja dispon�vel (available quantity)
	- Retornar erro caso o ID do filme n�o exista
	- Negar acesso caso o usu�rio n�o esteja logado
returnMovie(idMovie)
	- Devolver o filme (incrementar available quantity)
	- Retornar erro caso o usu�rio devolva um filme que ele n�o tenha alugado (verificar na tabela rent_movie retornar erro caso n�o tenha o registro dele)
	- Retornar erro caso o usu�rio tente devolve o mesmo filme mais de uma vez (verificar o return_at pra aquele registro)
	- Retornar erro caso o filme n�o exista
	- Negar acesso caso o usu�rio n�o esteja logado 
searchMovie(movieName)
	- Retornar filme
	- Erro filme n�o encontrado
	- Negar acesso caso o usu�rio n�o esteja logado


Todo
- Esquema do Banco x
- Estrutura da API x
- Configs da API  x
- Camada Service 
	- registerUser(user)
	- login(email, password)
	- logoff(user)
	- availableMoviesList() 
	- rentMovie(idMovie)
	- returnMovie(idMovie)
	- searchMovie(movieName)
- Camada controller
	- registerUser(user)
	- login(email, password)
	- logoff(user)
	- availableMoviesList()
	- rentMovie(idMovie)
	- returnMovie(idMovie)
	- searchMovie(movieName)
- Rotas 
- Adicionar npm start e nodemon
- Proteger rotas
- Testar autentica��o
- Testar rotas
- Fazer java doc para cada arquivo de camada
- Fazer java doc para as fun��es dificeis de entender
- Criar documenta��o com as rotas e par�metros a receber
- Gerar esquema do banco
- Criar seed com alguns filmes padr�es
- Empacotar tudo
- Botar no github com arquivo Readme de como instalar